[project]
name = "zeroconf"
version = "0.147.0"
description = "Stub zeroconf for ARM without wheels"
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["zeroconf"]
